platform :ios, '13.0'

target 'Bootstrap' do

  use_frameworks!
  
  # Need one pod
  pod 'Forgeries'

  target 'BootstrapTests' do
    pod 'Nimble-Snapshots', :path => "../"
    pod 'Quick'
  end

end
