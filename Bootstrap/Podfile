use_frameworks!

# Need one pod
pod 'Forgeries'

target 'BootstrapTests' do
  pod 'Quick', '~> 0.9.3'
  pod 'Nimble', '~> 4.1.0'

  pod 'Nimble-Snapshots/DynamicType', :path => "../"
  pod 'Nimble-Snapshots/DynamicSize', :path => "../"
end
